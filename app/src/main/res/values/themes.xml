<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.AndroidLessons" parent="Theme.MaterialComponents.DayNight.Bridge">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:colorPrimaryDark">@color/my_primary</item>
        <item name="colorPrimary">@color/my_primary</item>
    </style>

    <style name="Theme.AndroidLessons" parent="Base.Theme.AndroidLessons" />
</resources>